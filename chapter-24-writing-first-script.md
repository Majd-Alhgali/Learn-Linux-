# الفصل 24: كتابة أول سكريبت (Writing Your First Script)

> 📚 **مرجع الدرس:** كتاب "The Linux Command Line" - الفصل 24

---

## 🎯 لماذا نكتب السكربتات؟ (Why Write Scripts?)

تخيل أنك تقوم بنفس المهام يومياً: تنظيف ملفات مؤقتة، نسخ احتياطي، أو فحص النظام. بدلاً من كتابة نفس الأوامر كل مرة، نكتب **Shell Script** ينفذها تلقائياً بأمر واحد.

**السكربت = سلسلة أوامر محفوظة في ملف نصي قابل للتنفيذ.**

---

## 1️⃣ سطر الشيبانغ (Shebang Line) - أهم سطر في السكربت

### ما هو الـ Shebang؟

```bash
#!/bin/bash
```

### تشريح السطر (Anatomy):

| الجزء | المعنى |
|-------|--------|
| `#!` | رمز الشيبانغ (Shebang) - يخبر النظام أن هذا ملف قابل للتنفيذ |
| `/bin/bash` | المسار الكامل للمفسر (Interpreter) الذي سينفذ السكربت |

### 🔴 لماذا الشيبانغ مهم جداً؟

عندما تشغل سكربت بدون شيبانغ، النظام **لا يعرف** أي Shell يستخدم لتنفيذه! قد يستخدم `sh` أو `dash` بدلاً من `bash`، مما يسبب أخطاء لأن بعض الأوامر تعمل فقط في `bash`.

**القاعدة الذهبية:** دائماً ضع `#!/bin/bash` كأول سطر في كل سكربت.

### أمثلة على Shebang مختلفة:

```bash
#!/bin/bash      # سكربت Bash (الأكثر شيوعاً)
#!/bin/sh        # سكربت POSIX Shell
#!/usr/bin/python3   # سكربت Python
#!/usr/bin/env bash  # طريقة محمولة (Portable) للعثور على bash
```

---

## 2️⃣ صلاحيات الملفات (File Permissions) و chmod +x

### المشكلة:

عند إنشاء ملف نصي، يكون بشكل افتراضي **غير قابل للتنفيذ**. النظام يعتبره ملف نص عادي.

### الحل: منح صلاحية التنفيذ (Execute Permission)

```bash
chmod +x script_name.sh
```

### تشريح الأمر:

| الجزء | المعنى |
|-------|--------|
| `chmod` | أمر تغيير الصلاحيات (Change Mode) |
| `+x` | إضافة (+) صلاحية التنفيذ (x = execute) |
| `script_name.sh` | اسم الملف المستهدف |

### 🔍 كيف نتحقق من الصلاحيات؟

```bash
ls -l script_name.sh
```

**قبل chmod +x:**
```
-rw-r--r--  1 user user  45 Jan 30 10:00 script_name.sh
```

**بعد chmod +x:**
```
-rwxr-xr-x  1 user user  45 Jan 30 10:00 script_name.sh
```

لاحظ ظهور الحرف `x` في الصلاحيات! هذا يعني أن الملف أصبح قابلاً للتنفيذ.

---

## 3️⃣ متغير البيئة PATH وتنظيم السكربتات

### ما هو PATH؟

عندما تكتب أمراً مثل `ls`، كيف يعرف النظام أين يجده؟

الجواب: متغير البيئة **PATH** يحتوي على قائمة المجلدات التي يبحث فيها النظام عن الأوامر.

```bash
echo $PATH
```

**مثال على النتيجة:**
```
/usr/local/bin:/usr/bin:/bin:/usr/local/sbin:/usr/sbin:/home/user/bin
```

المجلدات مفصولة بـ `:` (نقطتين).

### 🔴 لماذا ننشئ مجلد ~/bin؟

بدلاً من وضع السكربتات في أماكن متفرقة، نضعها في مجلد مخصص:
- **التنظيم:** كل السكربتات الشخصية في مكان واحد
- **سهولة التشغيل:** تشغيل السكربت من أي مكان بدون كتابة المسار الكامل

### خطوات إنشاء مجلد ~/bin وإضافته للـ PATH:

#### الخطوة 1: إنشاء المجلد

```bash
mkdir -p ~/bin
```

> 💡 الخيار `-p` يمنع ظهور خطأ إذا كان المجلد موجوداً مسبقاً

#### الخطوة 2: التحقق من إضافة المجلد للـ PATH

في معظم توزيعات Linux الحديثة، المجلد `~/bin` يُضاف تلقائياً إذا كان موجوداً. للتحقق:

```bash
echo $PATH | grep -q "$HOME/bin" && echo "موجود في PATH" || echo "غير موجود"
```

#### الخطوة 3: إضافة المجلد للـ PATH (إذا لم يكن موجوداً)

أضف هذا السطر إلى ملف `~/.bashrc`:

```bash
echo 'export PATH="$HOME/bin:$PATH"' >> ~/.bashrc
```

ثم أعد تحميل الإعدادات:

```bash
source ~/.bashrc
```

---

## 4️⃣ المثال العملي: سكربت Hello World

### الخطوة 1: إنشاء السكربت

```bash
nano ~/bin/hello_world
```

اكتب المحتوى التالي:

```bash
#!/bin/bash

# My first shell script
# سكربتي الأول

echo "Hello, World!"
echo "مرحباً بك في عالم Shell Scripting!"
echo "اسم المستخدم: $USER"
echo "التاريخ الحالي: $(date)"
```

### تشريح السكربت:

| السطر | الشرح |
|-------|-------|
| `#!/bin/bash` | الشيبانغ - يحدد المفسر |
| `# comment` | تعليق - النظام يتجاهله (للتوثيق) |
| `echo "text"` | طباعة نص على الشاشة |
| `$USER` | متغير بيئة يحتوي اسم المستخدم |
| `$(date)` | تنفيذ أمر date ووضع نتيجته مكانه |

### الخطوة 2: منح صلاحية التنفيذ

```bash
chmod +x ~/bin/hello_world
```

### الخطوة 3: تشغيل السكربت

**الطريقة 1:** باستخدام المسار النسبي (من نفس المجلد)
```bash
cd ~/bin
./hello_world
```

**الطريقة 2:** باستخدام المسار الكامل
```bash
~/bin/hello_world
```

**الطريقة 3:** مباشرة (إذا كان ~/bin في PATH)
```bash
hello_world
```

### النتيجة المتوقعة:

```
Hello, World!
مرحباً بك في عالم Shell Scripting!
اسم المستخدم: majd
التاريخ الحالي: Fri Jan 30 10:30:00 UTC 2026
```

---

## 5️⃣ لماذا نكتب `./` قبل اسم السكربت؟

عندما تكتب أمراً، النظام يبحث عنه **فقط** في مجلدات PATH. المجلد الحالي (`.`) ليس في PATH افتراضياً لأسباب أمنية.

```bash
./hello_world
```

- `.` = المجلد الحالي (Current Directory)
- `/` = فاصل المسار
- `hello_world` = اسم السكربت

بدون `./`، النظام يبحث في PATH ولا يجد السكربت!

---

## 🏆 التحدي العملي (Mini-Project)

### المهمة: إنشاء سكربت "System Info"

أنشئ سكربتاً باسم `sysinfo` يعرض معلومات النظام التالية:

1. **اسم الجهاز (Hostname)**
2. **نظام التشغيل (OS)**
3. **وقت تشغيل النظام (Uptime)**
4. **المستخدمين المتصلين (Users)**
5. **استخدام القرص (Disk Usage)**

### الخطوات المطلوبة:

```bash
# 1. إنشاء مجلد bin إذا لم يكن موجوداً
mkdir -p ~/bin

# 2. إنشاء السكربت
nano ~/bin/sysinfo

# 3. كتابة المحتوى (استخدم الأوامر التالية):
#    - hostname
#    - uname -o
#    - uptime
#    - who
#    - df -h

# 4. منح صلاحية التنفيذ
chmod +x ~/bin/sysinfo

# 5. تشغيل السكربت
~/bin/sysinfo
```

### 💡 تلميح لمحتوى السكربت:

```bash
#!/bin/bash

echo "========== System Information =========="
echo "Hostname: $(hostname)"
echo "OS: $(uname -o)"
echo "Uptime: $(uptime -p)"
echo ""
echo "--- Logged in Users ---"
who
echo ""
echo "--- Disk Usage ---"
df -h /
echo "========================================"
```

---

## ✅ ملخص الدرس

| المفهوم | الأمر/الصيغة |
|---------|--------------|
| سطر الشيبانغ | `#!/bin/bash` |
| منح صلاحية التنفيذ | `chmod +x script_name` |
| إنشاء مجلد bin | `mkdir -p ~/bin` |
| تشغيل سكربت محلي | `./script_name` |
| عرض PATH | `echo $PATH` |

---

## 📝 أسئلة للمراجعة

1. ماذا يحدث إذا نسيت كتابة سطر الشيبانغ؟
2. لماذا لا نستطيع تشغيل السكربت بكتابة اسمه مباشرة بدون `./`؟
3. ما الفرق بين `chmod +x` و `chmod 755`؟

---

> 🎉 **تهانينا!** لقد كتبت أول سكربت لك. في الدروس القادمة سنتعلم المتغيرات، الشروط، والحلقات لبناء سكربتات أكثر تعقيداً!
